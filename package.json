{
  "name": "subrass-demo",
  "module": "server.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "wasm-pack build --target web --out-dir pkg",
    "dev": "bun --watch server.ts",
    "start": "bun server.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.0"
  }
}
